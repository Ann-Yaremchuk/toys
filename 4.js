'use strict';
/*#4. Соединение чисел
Как с помощью метода join превратить массив [3, 2, 1] в строку
"3 больше, чем 2 больше, чем 1"?*/

var arrayStart = [3,2,1];
var statemant = arrayStart.join('больше, чем');
console.log(statemant);
 